@model IEnumerable<ESTA.Models.Level>

@{
    ViewData["Title"] = "Index";
}

<h2>@localizer["levels"]</h2>

@*<p>
    <a class="btn btn-primary" asp-action="AddLevel">@localizer["newlevel"]</a>
</p>
*@

<div class="rounded-div">



<table class="table">
    <thead>
        <tr>
          @*  <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.TypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
         @*   <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.TypeName)
            </td>
            <td>
                    @Html.ActionLink(localizer["edit"], "EditLevel",new { id=item.Id},new { @class="btn btn-success"})
                @*|
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
            </td>
        </tr>
}
    </tbody>
</table>
</div>